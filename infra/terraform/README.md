# Terraform Placeholder Layout

This scaffold is intentionally light. It provides the shape for future AWS infrastructure without committing to a full implementation yet.

- `modules/ecs-service`: placeholder reusable ECS service module
- `modules/scheduled-task`: placeholder EventBridge scheduled ECS task module
- `environments/staging`: staging composition
- `environments/production`: production composition
