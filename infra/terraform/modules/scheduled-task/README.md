# Scheduled Task Module Placeholder

Intended use:

- EventBridge rule triggers a one-shot ECS task
- Initial use case is backup schedule configuration

Expected command override example:

```bash
pnpm --filter @payer-portal/server backups:configure
```
